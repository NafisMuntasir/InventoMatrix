#include "StatType.hpp"

// StatType is a scoped enum class (enum class StatType).
// Enum classes are fully defined in the header and require no implementation in a .cpp file.
// This file exists to maintain the consistent .hpp/.cpp pairing convention of the project.